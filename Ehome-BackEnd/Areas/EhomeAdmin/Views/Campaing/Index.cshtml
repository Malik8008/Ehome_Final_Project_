@using Ehome_BackEnd.Models
@model List<Campaing>

@{
    ViewData["Title"] = "Index";
}

<div class="table-responsive pt-3">
     <a asp-controller="campaing" asp-action="create" style="color:white;font-size:20px" class="mb-3 btn btn-success">Create campaing object</a>

    <table class="table table-dark">
        <thead>
            <tr>
                <th>
                    Image
                </th>
                <th>
                    Title
                </th>
                <th>
                    Subtitle
                </th>
                 <th>
                    Settings
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (Campaing campaing in Model)
            {
                <tr>
                    <td>
                        <img style="width: 150px; height:95px" src="~/assets/IMG/kampaniya-image/@campaing.Image">
                    </td>
                    <td>
                        @campaing.Title
                    </td>
                    <td>
                        @campaing.Subtitle
                    </td>
                    <td>
                        <div class="d-flex mt-4">
                            <div class="detail">
                                <a asp-controller="campaing" asp-action="Detail" asp-route-id="@campaing.Id" class="btn btn-primary"><i class="fa-solid fa-circle-info"></i></a>
                            </div>
                            <div class="edit">
                                <a asp-controller="campaing" asp-action="Edit" asp-route-id="@campaing.Id" class="btn btn-warning"><i class="fa-solid fa-pen-to-square" style="color:white;"></i>
                                    <i class="mdi mdi-tooltip-edit"></i>
                                </a>
                            </div>
                            <div class="delete">
                                <a asp-controller="campaing" asp-action="Delete" asp-route-id="@campaing.Id" class="btn btn-danger"> <i class="fa-solid fa-trash"></i></a>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


