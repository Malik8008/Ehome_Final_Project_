@using Ehome_BackEnd.Models
@model List<Country>

@{
    ViewData["Title"] = "Index";
}

<div class="table-responsive pt-3">
     <a asp-controller="country" asp-action="create" style="color:white;font-size:20px" class="mb-3 btn btn-success">Create country object</a>

    <table class="table table-dark">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                 <th>
                    Settings
                </th>
            </tr>
        </thead>
        <tbody>
           @foreach (var country in Model)
            {
                if(!country.IsDeleted)
                {
                    <tr>
                    <td>
                        @country.Name
                    </td>
                    <td>
                        <div class="d-flex">
                            <div class="edit">
                                <a asp-controller="country" asp-action="Edit" asp-route-id="@country.Id" class="btn btn-warning"><i class="fa-solid fa-pen-to-square" style="color:white;"></i>
                                    <i class="mdi mdi-tooltip-edit"></i>
                                </a>
                            </div>
                            <div class="delete">
                                <a asp-controller="country" asp-action="Delete" asp-route-id="@country.Id" class="btn btn-danger"> <i class="fa-solid fa-trash"></i></a>
                            </div>
                        </div>
                    </td>
                    </tr>
                }
                else
                {
                    <tr style="color:red !important">
                    <td>
                        @country.Name
                    </td>
                    <td>
                        <div class="d-flex">
                            <div class="edit">
                                <a asp-controller="country" asp-action="Edit" asp-route-id="@country.Id" class="btn btn-warning"><i class="fa-solid fa-pen-to-square" style="color:white;"></i>
                                    <i class="mdi mdi-tooltip-edit"></i>
                                </a>
                            </div>
                            <div class="delete">
                                <a asp-controller="country" asp-action="Delete" asp-route-id="@country.Id" class="btn btn-danger"> <i class="fa-solid fa-trash"></i></a>
                            </div>
                        </div>
                    </td>
                </tr>
                }
            }
        </tbody>
    </table>
</div>

