@using Ehome_BackEnd.Models
@model List<Category>

@{
    ViewData["Title"] = "Index";
}

<div class="table-responsive pt-3">
     <a asp-controller="category" asp-action="create" style="color:white;font-size:20px" class="mb-3 btn btn-success">Create category object</a>

    <table class="table table-dark">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                 <th>
                    Settings
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (Category category in Model)
            {
                <tr>
                    <td>
                        @category.Name
                    </td>
                    <td>
                        <div class="d-flex mt-4">
                            <div class="detail">
                                <a asp-controller="category" asp-action="Detail" asp-route-id="@category.Id" class="btn btn-primary"><i class="fa-solid fa-circle-info"></i></a>
                            </div>
                            <div class="edit">
                                <a asp-controller="cateory" asp-action="Edit" asp-route-id="@category.Id" class="btn btn-warning"><i class="fa-solid fa-pen-to-square" style="color:white;"></i>
                                    <i class="mdi mdi-tooltip-edit"></i>
                                </a>
                            </div>
                            <div class="delete">
                                <a asp-controller="category" asp-action="Delete" asp-route-id="@category.Id" class="btn btn-danger"> <i class="fa-solid fa-trash"></i></a>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


